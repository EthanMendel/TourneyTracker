<html>
    <head>

    </head>
    <body>
        <h1>{{tournament.name}}</h1>
        <h3>Teams Registered</h3>
        {{#each team}}
            <div>{{this}}</div>
        {{/each}}
        <button href="/registerTeam?tournamentId=tournament.id">Register Team</button>
    </body>
</html>